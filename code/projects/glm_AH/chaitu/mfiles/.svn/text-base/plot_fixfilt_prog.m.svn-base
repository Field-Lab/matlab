function out = plot_fixfilt_prog(x,j,basepars,stimpars,trainpars)

out = 0;
%subplot(1,2,1);
%plot(x(end-9:end));


1;

% commented out, edoi, 2012-01-05.
% offset = (j-1)*get_npars(basepars,size(trainpars.D,2))+1;
% 
% K = x(offset+2).*basepars.K{j};
% cla;
% if(basepars.n > 1)
%     imagesc(K',[-1 1].*max(eps,max(abs(K(:))))), axis tight, axis image;
% else
%     plot(0:stimpars.dt:(basepars.Mk-1)*stimpars.dt,K,'.-');
% end
% 
% title(sprintf('b=%0.5f stimulus filter norm: %0.5f',x(offset),norm(K,'fro')*sqrt(stimpars.dt)));

%subplot(1,2,2);
%plot(x(end-9:end));
%pause(0.1);

